<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MyButton">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:layout_margin">@dimen/widget_margin</item>
        <item name="android:layout_height">@dimen/widget_height</item>
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/widget_text_size_large</item>
    </style>

    <dimen name="widget_margin">8dp</dimen>
    <dimen name="widget_height">47dp</dimen>
    <dimen name="widget_text_size_large">20sp</dimen>
    <color name="white">#fff</color>
</resources>
